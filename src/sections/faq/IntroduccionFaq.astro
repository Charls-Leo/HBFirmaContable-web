---
---

<section class="section intro-faq" aria-labelledby="intro-faq-title">
  <div class="container intro-faq__inner">
    <h2 class="heading reveal" id="intro-faq-title">Información clara antes de comenzar</h2>
    <p class="reveal">
      Cada situación puede tener condiciones particulares que requieren un análisis a profundidad. Sin embargo, hemos reunido las dudas más comunes para que usted pueda avanzar con mayor confianza en su gestión contable y tributaria.
    </p>
  </div>
</section>

<style>
  .intro-faq {
    background: var(--color-white);
    padding-block: clamp(3.5rem, 6vw, 5rem);
  }

  .intro-faq__inner {
    max-width: 40rem;
    margin-inline: auto;
    text-align: center;
  }

  .heading {
    font-size: var(--tamano-3xl);
  }

  .intro-faq__inner p {
    margin: 1.25rem 0 0;
    color: color-mix(in srgb, var(--color-navy) 64%, transparent);
    font-size: 1rem;
    font-weight: 300;
    line-height: 1.7;
  }
</style>
