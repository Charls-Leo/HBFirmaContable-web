---
---

<section class="section aviso-documentos" aria-labelledby="aviso-documentos-title">
  <div class="container aviso-documentos__inner">
    <span class="eyebrow reveal">Protocolo de seguridad</span>
    <h2 class="heading reveal" id="aviso-documentos-title">¿Puedo enviar documentos contables desde este formulario?</h2>
    <p class="reveal">
      Por seguridad, este formulario está destinado únicamente al contacto inicial. La entrega de documentos confidenciales se coordina posteriormente mediante el canal seguro definido con la firma.
    </p>
  </div>
</section>

<style>
  .aviso-documentos {
    background: var(--color-white);
    padding-block: clamp(3.5rem, 6vw, 5rem);
  }

  .aviso-documentos__inner {
    max-width: 38rem;
    margin-inline: auto;
    text-align: center;
  }

  .heading {
    margin: 1rem 0 1.1rem;
    font-size: var(--tamano-2xl);
  }

  .aviso-documentos__inner p {
    margin: 0;
    color: color-mix(in srgb, var(--color-navy) 62%, transparent);
    font-size: 0.9375rem;
    font-weight: 300;
    line-height: 1.7;
  }
</style>
